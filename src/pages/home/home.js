import { Box } from "@mui/material";
import styled from "styled-components";

const Home = () => {
  return <StyledComponent>Home</StyledComponent>;
};

const StyledComponent = styled(Box)`
  display: flex;
  width: 100%;
`;

export default Home;
